import { AnyAction } from "redux";
import { IInventoryState } from ".";

const initialState: IInventoryState ={

}


export const inventoryReducer = (state: IInventoryState = initialState, action: AnyAction) => {

}